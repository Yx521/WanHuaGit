<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!--设置背景颜色-->
    <solid android:color="#e61616" />
    <!--这个是设置边框的粗细,以及边框颜色的-->
    <stroke android:width="0dp" android:color="#54ff00" />
    <!--这个是设置边距的-->
    <padding
        android:bottom="@dimen/dp_5"
        android:left="@dimen/dp_5"
        android:right="@dimen/dp_5"
        android:top="@dimen/dp_5" />
    <!-- 设置四个圆角的半径 -->
    <corners
        android:bottomLeftRadius="@dimen/dp_50"
        android:bottomRightRadius="@dimen/dp_50"
        android:topLeftRadius="@dimen/dp_50"
        android:topRightRadius="@dimen/dp_50" />
    <!-- 渐变 -->
    <!--这个是设置渐变色的,可选属性有: startColor:起始颜色 endColor:结束颜色 centerColor:中间颜色
    angle:方向角度,等于0时,从左到右,然后逆时针方向转,当angle = 90度时从下往上 type:设置渐变的类型-->
    <gradient
        android:angle="270"
        android:endColor="#F5F0F0"
        android:startColor="#F5F0F0"
        android:type="linear"/>
</shape>